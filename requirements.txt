# Core API — this is all Render/production needs.
fastapi==0.115.12
uvicorn==0.34.2
pydantic==2.11.3
python-multipart==0.0.20
pandas==2.2.3
matplotlib==3.9.4
reportlab==4.2.5
python-dotenv==1.0.1
requests==2.32.3
urllib3==2.2.3

# ── OPTIONAL: DistilBERT expense classifier ───────────────────────────────
# Only /api/upload-expenses ("Predictive Expense Analysis") uses this. main.py
# loads it lazily; every other endpoint (advisory, feasibility, finance, AI chat,
# schemes) works fine without it — the categorizer just returns "Unknown".
# The model weights (model.safetensors ~256 MB) are NOT in the repo, so this
# won't run on a fresh clone / Render anyway. For local use with the classifier:
#     pip install -r requirements-ml.txt
# and drop model.safetensors + label_encoder.pkl + config.json + tokenizer*.json
# into backend/.

# ── OPTIONAL: local-LLM offline fallback ──────────────────────────────────
#     pip install ollama==0.4.7
